package com.collections.priorityQueue;

public class PriorityQueueConcept {

	public static void main(String[] args) {
		
		// PriorityQueue
	/*
	 * PriorityQueue internally make use Min-Heap(Binary Heap)
	 * The lowest element it has Highest priority
	 * parent node is less than child node
	 
	 
	 | Method               | Description                                                       |
| -------------------- | ----------------------------------------------------------------- |
| `add(E e)`           | Inserts element into the queue (throws exception if full).        |
| `offer(E e)`         | Inserts element into the queue (returns `false` if full).         |
| `peek()`             | Retrieves but does **not remove** the head element.               |
| `poll()`             | Retrieves and **removes** the head element.                       |
| `remove()`           | Removes and returns the head element (throws exception if empty). |
| `contains(Object o)` | Checks if the queue contains the element.                         |
| `size()`             | Returns number of elements.                                       |
| `clear()`            | Removes all elements.                                             |
| `iterator()`         | Returns an iterator (no guaranteed order).                        |


     * add() adds the element to the queue nased on its priority
     * remove() the elements based on its priority
     * 
	 */
	

	}

}
