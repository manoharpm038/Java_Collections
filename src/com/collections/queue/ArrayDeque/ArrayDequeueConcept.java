package com.collections.queue.ArrayDeque;

public class ArrayDequeueConcept {

	public static void main(String[] args) {
		
		/*
		 ArrayDeque in Java
	Definition

	* ArrayDeque (Array Double Ended Queue) 
	* It is a resizable array-based implementation of the Deque (Double Ended Queue) interface.
	* It allows insertion and deletion from both ends — front and rear.
	* Insertion in between the elements is not possible
	
	| Operation                 | Method                                         | Description                |
| ------------------------- | ---------------------------------------------- | -------------------------- |
| Add element at end        | `addLast(E e)` / `add(E e)` / `offerLast(E e)` | Adds element at tail       |
| Add element at front      | `addFirst(E e)` / `offerFirst(E e)`            | Adds element at head       |
| Remove element from front | `removeFirst()` / `pollFirst()`                | Removes head element       |
| Remove element from end   | `removeLast()` / `pollLast()`                  | Removes tail element       |
| Get first element         | `getFirst()` / `peekFirst()`                   | Retrieves head (no remove) |
| Get last element          | `getLast()` / `peekLast()`                     | Retrieves tail (no remove) |
| Check size                | `size()`                                       | Returns number of elements |
| Check empty               | `isEmpty()`                                    | Returns true if empty      |
| Remove all elements       | `clear()`                                      | Clears deque               |



		 */

	}
}
